import React from "react";
import styled from "styled-components";

const DayWrapper = styled.article`
text-align:center;
`;
const Test = () => {
	return (
		<div>
			<DayWrapper />
		</div>
	)
}

export default Test;